apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-storage-pvc
  namespace: fintech-monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-database-sc
  resources:
    requests:
      storage: 2Gi
      